// import React from 'react';
import { render, screen } from '@testing-library/react';
// import App from './App';

test('renders learn react link', () => {
  expect('foo').toBe('foo')

  // render(<App />);
  // const linkElement = screen.getByText(/top page/i);
  // expect(linkElement).toBeInTheDocument();
});
